# Copy this to .env and fill in your backend URL
# During dev the vite proxy handles /api → http://localhost:8081
# For production builds set this to your real backend URL:
VITE_API_BASE_URL=http://localhost:8081
